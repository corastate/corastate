/**
 * Public surface of @corastate/connector-sdk.
 *
 * The SDK is types only today. The framework (in @corastate/core) imports
 * these types to talk to connectors uniformly.
 */

export * from './types.js';
